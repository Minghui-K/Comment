server:
  port: 8081
spring:
  application:
    name: ke_comment
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/ke_comment?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: kmh200088

  cache:
    type: redis
    redis:
      time-to-live: 1800000 #设置缓存数据的过期时间

  redis:
    host: 127.0.0.1
    port: 6379
    #password: 123321
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 1
        time-between-eviction-runs: 10s
  jackson:
    default-property-inclusion: non_null # JSON处理时忽略非空字段
mybatis-plus:
  type-aliases-package: com.ke.comment.entity # 别名扫描包
logging:
  level:
    com.ke: debug